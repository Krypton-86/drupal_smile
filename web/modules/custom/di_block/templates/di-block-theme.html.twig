
<div>
  <h2>{{ list['info']['current_user']}}</h2>
  <h2>{{ list['info']['user_info']['name'] }}</h2>
  <h2>{{ list['info']['user_info']['date'] }}</h2>
</div><br>
<div class="di">{{ list['info']['all_active'] }}</div>
<div class="di">{{ list['info']['number_of_user'] }}</div>
<div><h3>{{ list['info']['content'] }}</h3></div>
<div>{{ list['info']['node'] }}</div>
